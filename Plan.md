# VC Planner — 개발 계획서 (Plan.md)

> **서비스명**: VC Planner
> **한 줄 설명**: 비개발자가 단계별 질문·플로우차트·와이어프레임으로 서비스를 설계하고, 어떤 AI Coding Agent에도 붙여넣을 수 있는 표준 Markdown 개발 명세서(`Prompt.md` + Agent 규칙 파일)를 생성하는 웹 서비스
> **문서 버전**: v1.1
> **변경 이력**:
> - v1.0 — 초안
> - v1.1 — ① `[✨ AI 추천]` 버튼 의미 확정(다운스트림 Agent 위임) ② Agent 범용성(도구 비종속 표준 Markdown) 요구사항 추가 ③ Cloudflare Pages 배포·라이선스·소스 보호 가드레일 추가 ④ 개발 환경 버전 정보 확정 기록

---

## 0. 이 문서의 목적

이 문서는 VC Planner를 **어떤 순서로, 어디까지, 어떤 규칙 아래** 만들 것인지 정의한다.
구현을 맡는 AI Agent와 사용자 모두 이 문서를 단일 기준(Single Source of Truth)으로 삼는다.

- 명세(무엇을 만드나) → `docs/spec.md` 또는 본 문서 2~6장
- 실행 계획(어떤 순서로 만드나) → 본 문서 7장
- Agent 행동 규칙 → 본 문서 1장 + 루트 규칙 파일(`.cursorrules` / `AGENTS.md`)
- 산출물 포맷 규격 → 본 문서 11장
- 배포·라이선스·소스 보호 → 본 문서 12장

---

## 1. 자기 준용 원칙 (Self-Application)

VC Planner는 "좋은 명세서를 만들어주는 도구"이므로, **자기 자신도 그 명세 규칙을 그대로 지켜서** 개발한다.
명세서 Step 2의 추천값과 Step 3의 선택 항목을 이 프로젝트 개발에 그대로 적용한다.

### 1-1. Step 2 준용 — 기술 및 저장 방식

| 항목 | 채택값 | 근거 |
|---|---|---|
| 앱 형태 | **Next.js (App Router) + Tailwind CSS** | Step 2 추천값. 모던 웹앱, 정적 배포 가능 |
| 데이터 저장 | **브라우저 LocalStorage** | Step 2 추천값. 서버·비용·회원가입 없이 즉시 동작 |
| 백엔드 | **없음 (완결형 클라이언트 앱)** | 외부 AI API 연동 없이 브라우저 상태만으로 동작 |
| 배포 | **Cloudflare Pages (Static Export)** | 백엔드가 없으므로 정적 배포로 충분. 무료 [G3], 12장 참조 |
| 라이선스 | **BUSL-1.1 (source-available)** | 서비스 이용은 자유, 소스 재배포·재가공은 제한. 12-3장 참조 |

### 1-2. Step 3 준용 — AI 규칙 & 가드레일

구현 Agent는 아래 규칙을 **모든 작업에서** 준수한다. 이 목록은 그대로 프로젝트 루트 규칙 파일로 옮긴다 — 내용은 동일하고 파일명만 도구에 맞춘다(`.cursorrules` / `AGENTS.md` / `.windsurfrules`, 11-4장 참조).

- **AI 역할**: 친절하고 유능한 수석 풀스택 개발자
- **[G1] 🌐 인코딩 & 크로스 플랫폼 호환성 (UTF-8 필수)**
  모든 소스 파일은 UTF-8(BOM 없음)로 저장한다. 파일 생성·다운로드·클립보드 복사 전 경로에서 한글이 깨지지 않도록 처리하고, 경로 구분자를 하드코딩하지 않는다. 개발 환경은 Windows이므로 PowerShell 기준으로 명령을 작성한다. 세부 준수 항목은 3-5장을 따른다.
- **[G2] 📦 패키지 버전 충돌 방지**
  React / Next.js 메이저 버전과 호환이 검증된 조합만 설치한다. 특히 `react-grid-layout` 계열은 React 19 호환성 이슈가 있으므로 3장 "버전 고정 정책"을 따른다.
- **[G3] 💵 오픈소스 & 무료 우선**
  전 구성요소를 오픈소스/무료로 구성한다. 유료 요소가 필요하면 착수 전에 사유를 설명하고 승인을 받는다.
- **[G4] 📐 와이어프레임 & 명세서 엄격 준수**
  본 문서에 정의된 화면 구성·데이터 항목·예외 처리를 임의로 바꾸지 않는다. 변경이 필요하면 먼저 제안하고 승인 후 본 문서를 갱신한다.
- **[G5] ⚠️ 작업 단위 쪼개기**
  한 번에 전체를 구현하지 않는다. `기본 틀 → 기능 1개 추가 → 동작 확인` 순으로 진행하며, 7장 Phase 경계를 넘지 않는다.
- **[G6] ⚠️ 기존 코드 보호**
  파일 전체 재작성을 금지한다. 수정이 필요한 부분만 부분 편집(Diff)한다.
- **[G7] ⚠️ 에러 대응 절차**
  에러 발생 시 바로 코드를 고치지 않는다. ① 원인 설명 → ② 수정 계획 제시 → ③ 코드 작성 순서를 지킨다.
- **[G8] ⚠️ 패키지 설치 승인**
  새 패키지 설치 전 반드시 이유를 설명하고 승인을 받는다.
- **[G9] ⚠️ 비개발자 친화적 대화**
  전문 용어 대신 쉬운 말로 설명하고, 핵심 로직에는 한국어 주석을 단다.
- **[G10] 🔒 보안 및 규정 준수**
  API 키·비밀값을 코드에 하드코딩하지 않는다. VC Planner 자체는 외부 전송이 없는 로컬 저장 구조이므로, 사용자 입력이 브라우저 밖으로 나가지 않도록 유지한다.
- **[G11] 🤖 Agent 범용성 (도구 비종속)**
  최종 산출물은 특정 IDE·도구에 종속되지 않는 **표준 Markdown(CommonMark)** 으로 생성한다. Cursor / Claude Code / Lovable / Bolt.new / Windsurf 등 어느 Agent에 붙여넣어도 동일하게 해석되도록 11장의 공통 프롬프트 구조를 따른다. 특정 도구 전용 문법(Cursor `@파일참조`, Windsurf 전용 지시자 등)은 본문에 쓰지 않고, 필요하면 도구별 부록 섹션으로 분리한다.
- **[G12] 🚀 배포 & 소스 보호**
  Cloudflare Pages(Static Export) 환경에 그대로 올릴 수 있는 빌드 구성을 유지한다. 서버 런타임(API Routes, `next/image` 최적화 서버, 서버 액션 등)에 의존하는 코드를 추가하지 않는다. 프로덕션 빌드는 소스맵을 끄고 최소화(minify)하며, 라이선스 고지를 푸터와 명세서에 항상 포함한다. 세부 규칙은 12장을 따른다.

---

## 2. 대상 사용자와 UX 원칙

### 2-1. 대상
웹 개발 용어를 잘 모르는 비개발자, 입문자, 그리고 도메인 지식은 깊지만 개발은 처음인 실무 전문가.

### 2-2. UX 핵심 원칙

1. **마우스 선택 우선**
   모든 항목은 라디오 / 체크박스 / 드롭다운만으로 완성 가능해야 한다. 키보드 입력이 필수인 항목은 서비스 이름·한 줄 설명 정도로 최소화한다.

2. **모든 선택지 하단에 `[✏️ 직접 입력 / 기타]`**
   도메인 지식이 있는 사용자가 세부 요구사항을 직접 쓸 수 있어야 한다. 선택 시 텍스트 입력창이 자연스럽게 펼쳐지고, 해제하면 접힌다. 입력값은 유지하되 최종 출력에는 선택 상태일 때만 반영한다.

3. **쉬운 용어 + 툴팁**
   `LocalStorage` 같은 용어는 "내 브라우저에 저장(서버 없이 무료)"처럼 풀어 쓰고, 원어는 툴팁으로 보조한다.

4. **막히면 누를 수 있는 버튼 — 두 종류를 구분한다**

   VC Planner는 **어떤 경우에도 외부 AI를 호출하지 않는다.** 막힌 사용자를 돕는 장치는 성격이 다른 두 가지이며, UI에서도 시각적으로 구분한다.

   | 버튼 | 의미 | 동작 | 출력물 반영 |
   |---|---|---|---|
   | `[🤖 AI 추천 받기]` | **이 항목은 지금 정하지 않고, 나중에 내 개발 Agent에게 추천받겠다** | 해당 항목을 `위임(delegated)` 상태로 표시. 값 자체는 비워 둔다 | `Prompt.md`에 "이 항목은 Agent가 판단해 제안해 달라"는 **지시문**으로 출력 |
   | `[✨ 잘 모르겠어요 (기본값 적용)]` | **일반적으로 무난한 값으로 지금 채워 달라** | 사전 정의된 로컬 프리셋으로 즉시 값 채움 | 채워진 값이 그대로 출력 |

   - `[🤖 AI 추천 받기]`는 **판단 위임 마커**다. VC Planner가 답을 만들어내지 않고, 결정을 Prompt.md를 받는 Agent(Cursor / Claude Code / Lovable 등)에게 넘긴다.
   - 위임 상태에서도 사용자는 원하면 힌트를 적을 수 있다(예: "비용은 최소로"). 이 힌트는 위임 지시문 안에 "사용자 선호"로 함께 실린다.
   - `[✨ 기본 CRUD 흐름 불러오기]`는 프리셋 계열이며 플로우차트 전용이다.
   - 위임 항목이 하나라도 있으면 Step 4 상단에 "N개 항목을 Agent 판단에 맡겼습니다" 요약 배너를 띄운다.

5. **실시간 피드백**
   화면 우측(모바일은 하단)에 `Prompt.md` 미리보기가 항상 떠 있고, 입력이 바뀌면 즉시 갱신된다.

6. **다크 모드 기본**
   모던하고 절제된 다크 인터페이스. 강조색은 1개로 제한하고, 경고·민감 데이터 표시에만 보조색을 쓴다.

---

## 3. 기술 스택 및 버전 고정 정책

### 3-0. 개발 환경 (실측 확인 완료)

| 항목 | 확인값 | 비고 |
|---|---|---|
| OS | Windows 10/11 (win32 10.0.26200) | 셸은 PowerShell 기준 |
| Node.js | **v24.18.0** | Next.js 15/16 모두 요구사항(≥ 20) 충족 |
| npm | **11.16.0** | npm 12 업그레이드 알림이 뜨지만 업그레이드하지 않는다 [G2] |
| 작업 경로 | `C:\Users\lky94\VC_Planner` | 경로에 공백·한글 없음 (인코딩 사고 위험 낮음) |

npm 레지스트리 실측 최신 버전(참고용): `next 16.2.12` / `react 19.2.8` / `react-grid-layout 2.2.3` / `@xyflow/react 12.11.2` / `html2pdf.js 0.14.0` / `tailwindcss 4.3.3`.

### 3-1. 채택 스택

| 구분 | 선택 | 용도 |
|---|---|---|
| 프레임워크 | Next.js (App Router) | 페이지 구조, 정적 빌드 |
| 언어 | TypeScript | 상태 스키마 타입 안전성 |
| 스타일 | Tailwind CSS | 다크 모드, 반응형 |
| 아이콘 | `lucide-react` | UI 아이콘 |
| 플로우차트 | `@xyflow/react` v12.11+ | Step 1-2 업무 흐름도 |
| 와이어프레임 | `react-grid-layout` v2.2+ | Step 1-3 화면 레이아웃 |
| PDF | `html2pdf.js` (`jspdf` + `html2canvas`) | Step 4 PDF 리포트 |
| 상태 관리 | React Context + `useReducer` (필요 시 `zustand`) | 위저드 전역 상태 |

### 3-2. 버전 고정 정책 [G2]

**결정**: **Next.js 15 (App Router) + React 19** 조합으로 진행한다.

Next.js 16이 이미 공개되어 있으나, 본 프로젝트는 `@xyflow/react`·`react-grid-layout`·`html2pdf.js` 같은 브라우저 의존 라이브러리를 Static Export 위에서 돌려야 하므로 **검증 사례가 많은 15 계열을 유지**한다. 16으로 올리는 것은 v1 완료 후 별도 승인 사항으로 미룬다 [G2][G8].

두 핵심 라이브러리 모두 이 조합에서 우회 플래그 없이 설치·동작한다.

- `react-grid-layout` **v2.x** — 2025년 12월 v2.0.0에서 TypeScript로 전면 재작성되었고 React 18+를 요구한다. peer dependency가 `react >= 16.3.0`이므로 React 19에서 충돌 없이 설치된다. v2는 타입 정의를 자체 포함하므로 `@types/react-grid-layout`을 **따로 설치하지 않는다**.
- `@xyflow/react` **v12.11+** — React 19 / Next.js 15를 공식 지원한다.

**v2 마이그레이션 주의 (구버전 예제 코드를 참고할 때)**

- UMD 번들이 제거되었다. 번들러(Next.js 기본 webpack/Turbopack)를 통해서만 사용한다.
- `verticalCompact` prop이 제거되었다. 대신 `compactType={null}`을 쓴다.
- 인터넷의 v1 기준 예제는 그대로 복사하면 동작하지 않을 수 있으므로, 공식 v2 문서를 기준으로 작성한다.

**설치 시 원칙**: `--legacy-peer-deps`나 `--force`로 경고를 덮지 않는다. peer dependency 충돌이 실제로 발생하면 우회하지 말고 원인을 보고한 뒤 승인을 받는다 [G2][G8].

**대비책**: 만약 `react-grid-layout` v2에서 예기치 못한 문제가 생기면 `@dnd-kit/core` + `react-rnd` 조합으로 교체한다. 단 그리드 스냅·충돌 회피를 직접 구현해야 해 Phase 5 공수가 늘어나므로, 교체는 사전 승인 후에만 한다.

### 3-3. 클라이언트 전용 처리
`@xyflow/react`, `react-grid-layout`, `html2pdf.js`는 모두 브라우저 API에 의존한다.
→ `next/dynamic`의 `ssr: false`로 동적 로드하고, 컴포넌트에 `'use client'`를 명시한다.

### 3-4. LocalStorage 하이드레이션
서버 렌더 결과와 LocalStorage 복원 결과가 달라 하이드레이션 불일치가 발생할 수 있다.
→ 최초 렌더는 기본값으로 그리고, `useEffect` 안에서 저장값을 복원하는 `mounted` 가드를 공통 훅으로 만든다.

### 3-5. 인코딩 준수 체크리스트 [G1]

한글 깨짐은 이 프로젝트에서 가장 발생 확률이 높은 실패다. 아래를 규칙으로 고정한다.

**저장소 / 소스 파일**

- 모든 소스·문서 파일: **UTF-8, BOM 없음, LF 개행**.
- 루트에 `.editorconfig`를 두어 강제한다: `charset = utf-8`, `end_of_line = lf`, `insert_final_newline = true`.
- 루트에 `.gitattributes`를 두어 `* text=auto eol=lf`로 통일한다. Windows CRLF가 섞여 들어가는 것을 막는다.
- HTML `<head>`에 `<meta charset="utf-8">`가 들어가는지 확인한다(Next.js 기본 제공).

**터미널 (PowerShell)**

- PowerShell 기본 출력 코드페이지가 949(EUC-KR)면 npm/Next 로그의 한글이 깨진다. 개발 시작 시 아래로 UTF-8 고정을 권장한다.

```powershell
chcp 65001
$OutputEncoding = [System.Text.UTF8Encoding]::new()
[Console]::OutputEncoding = [System.Text.UTF8Encoding]::new()
```

- 이는 **표시 문제일 뿐 파일 내용과는 무관**하다. 터미널에서 한글이 깨져 보인다고 소스 파일을 건드리지 않는다.
- 파일 생성·수정은 셸 리다이렉션(`>`, `echo`, heredoc)이 아니라 편집 도구로 한다. PowerShell 리다이렉션은 UTF-16LE로 쓰는 경우가 있다.

**런타임 출력물 (가장 중요)**

- 파일 다운로드: `new Blob([text], { type: 'text/markdown;charset=utf-8' })`. **BOM을 붙이지 않는다.**
- `.cursorrules` / `AGENTS.md` 등 Agent 설정 파일은 BOM이 파싱을 방해할 수 있으므로 특히 BOM 금지.
- 클립보드 복사는 `navigator.clipboard.writeText()`를 쓴다. 비보안 컨텍스트(HTTP)에서는 사용할 수 없으므로 실패 시 `<textarea>` 폴백과 안내 토스트를 둔다.
- 다운로드 파일명에 한글이 들어갈 수 있다(서비스 이름 기반). 파일명은 한글을 허용하되 `\ / : * ? " < > |` 등 Windows 금지 문자만 `_`로 치환한다.
- **검증 방법**: 다운로드한 `.md`를 Windows 메모장과 VS Code에서 각각 열어 한글이 정상인지 확인한다(Phase 6 DoD).

---

## 4. 화면 구조 (IA)

```
┌──────────────────────────────────────────────────────────────┐
│  [VC Planner]       Step ①─②─③─④ 진행바        [저장됨 ✓]  │
├───────────────────────────────────┬──────────────────────────┤
│                                   │                          │
│   Wizard 본문 (카드형)            │   Prompt.md 실시간       │
│   - Step 1: 서비스 개요/흐름/화면 │   미리보기               │
│   - Step 2: 기술 및 저장 방식     │   (스크롤 동기화)        │
│   - Step 3: AI 규칙 & 가드레일    │                          │
│   - Step 4: 최종 생성 & 추출      │   [탭] Prompt / rules    │
│                                   │                          │
├───────────────────────────────────┴──────────────────────────┤
│  [← 이전]                                    [다음 →]        │
├──────────────────────────────────────────────────────────────┤
│  © 2026 VC Planner · BUSL-1.1 · 무단 재배포/재가공 금지       │
│  입력 내용은 브라우저에만 저장됩니다                          │
└──────────────────────────────────────────────────────────────┘
```

- 데스크톱: 좌측 위저드 / 우측 미리보기 2단 분할
- 모바일·태블릿: 미리보기를 하단 시트로 전환
- 진행바의 각 Step은 클릭으로 이동 가능 (선형 강제 아님)
- 푸터는 전 화면 공통이며 라이선스 고지 + 로컬 저장 안내를 함께 노출한다 [G12]

---

## 5. 데이터 모델

### 5-1. 공통 선택값 타입

`[✏️ 직접 입력 / 기타]` 패턴이 전 화면에 반복되므로 **단일 타입으로 통일**한다.

```ts
/** 선택지 하나 */
type Option = {
  id: string;
  label: string;        // 비개발자용 쉬운 문구
  hint?: string;        // 툴팁 보조 설명
  recommended?: boolean;// [✨ 추천] 배지
};

/** 사용자의 선택 결과 (단일/다중 공용) */
type ChoiceValue = {
  selectedIds: string[];   // 선택된 옵션 id 목록
  customEnabled: boolean;  // [✏️ 직접 입력] 활성 여부
  customText: string;      // 직접 입력 내용

  // [🤖 AI 추천 받기] = 이 항목의 판단을 다운스트림 Agent에게 위임
  delegated: boolean;      // 위임 여부
  delegateHint: string;    // 위임 시 함께 전달할 사용자 선호 (선택)
};
```

**규칙 1**: 마크다운 생성기는 `selectedIds`의 라벨과 `customEnabled === true`일 때의 `customText`를 **둘 다** 출력해야 한다. 어느 한쪽도 누락되면 안 된다.

**규칙 2 (위임 우선)**: `delegated === true`이면 그 항목은 값 대신 **위임 지시문**으로 출력한다. 출력 형태는 11-3장 템플릿을 따른다.

```
- **화면 예외 처리**: 🤖 _AI 추천 요청_ — 이 항목은 사용자가 결정을 보류했습니다.
  프로젝트 성격에 맞는 방안을 2~3개 제안하고, 하나를 추천 이유와 함께 제시한 뒤 진행하세요.
  (사용자 선호: 구현이 단순한 쪽)
```

**규칙 3**: `delegated`와 실제 선택값은 상호배타로 다루지 않는다. 사용자가 위임을 켠 뒤 값을 고르면 위임이 자동 해제되고, 위임을 켜면 기존 선택값은 지우지 않고 보관만 한다(되돌리기 대비). 단 **출력에는 위임 지시문만** 나간다.

### 5-2. 전역 상태 스키마

```ts
type ProjectState = {
  meta: { version: string; updatedAt: string };

  // Step 1
  basic: {
    serviceName: string;
    oneLiner: string;
    domain: ChoiceValue;        // 일반IT/금융회계/공공행정/헬스케어/커머스/직접입력
    sensitiveData: ChoiceValue; // 일반/민감포함/직접입력
  };
  flowchart: { nodes: FlowNode[]; edges: FlowEdge[] };
  wireframe: { items: WireframeItem[]; cols: number; rowHeight: number };
  dataIO: { input: ChoiceValue; output: ChoiceValue };
  edgeCases: { emptyState: ChoiceValue; errorState: ChoiceValue };

  // Step 2
  tech: { appType: ChoiceValue; storage: ChoiceValue };

  // Step 3
  agentRules: { role: ChoiceValue; guardrails: ChoiceValue };

  // Step 4 — 출력 옵션
  output: {
    /** 본문은 항상 도구 비종속. 이 옵션은 "부록"으로 도구별 파일을 덧붙일지 여부 [G11] */
    includeToolAppendix: boolean;
    /** 부록에 포함할 대상 (cursor / claude / windsurf / lovable / bolt) */
    targetAgents: string[];
  };
};
```

### 5-3. 와이어프레임 아이템

```ts
type WireframeItem = {
  i: string;                 // react-grid-layout 키
  x: number; y: number;
  w: number; h: number;
  type: 'navbar' | 'sidebar' | 'search' | 'table'
      | 'kpi' | 'buttons' | 'chart' | 'custom';
  label: string;             // 사용자가 붙인 명칭
};
```

### 5-4. 영속화
- 키: `vc-planner:project:v1`
- 저장 시점: 상태 변경 후 500ms 디바운스
- 헤더에 "저장됨 ✓" 표시
- `[초기화]` 버튼은 확인 모달을 거친다

---

## 6. 기능 상세

### 6-1. Step 1 — 서비스 개요, Flowchart, 상세 명세

**1-1. 기본 정보**
서비스 이름, 한 줄 설명(텍스트) / 서비스 분야, 민감 데이터 처리 여부(선택).
민감 데이터를 선택하면 Step 3의 `[🔒 보안 및 규정 준수]` 가드레일이 **자동 체크되고 해제 불가**로 잠긴다. 잠긴 이유를 툴팁으로 안내한다.

**1-2. Flowchart 에디터 (React Flow)**
- 노드 타입: `시작점` / `기능·페이지` / `데이터 저장·DB` / `조건 분기` / `종료점`
- 좌측 팔레트에서 캔버스로 드래그하여 추가, 노드 더블클릭으로 이름 편집, 핸들을 끌어 연결
- `[✨ 기본 CRUD 서비스 흐름 자동 불러오기]`: 시작 → 목록 → 등록/수정/삭제 → DB → 종료 프리셋 삽입
- 마크다운 변환: 노드·엣지 목록을 텍스트 흐름(`A → B → C`) 및 Mermaid `flowchart TD` 블록으로 출력

**1-3. Wireframe 에디터 (react-grid-layout)**
- 팔레트: 네비게이션바 / 사이드바 / 검색창 / 데이터 테이블 / 요약 카드(KPI) / 버튼 그룹 / 차트 영역 / 직접 입력
- 배치·크기 조절·라벨링 지원. 이벤트 동작은 구현하지 않는다(레이아웃 전용).
- **ASCII 변환**: 그리드 좌표를 스캔해 박스 드로잉 문자로 레이아웃 도식을 만들고, 아래에 컴포넌트 목록 표를 붙인다.

```
┌──────────────────────────────────────────┐
│ [네비게이션바] 서비스 로고 / 메뉴        │
├───────────┬──────────────────────────────┤
│ [사이드바]│ [검색창] 조건 입력           │
│  메뉴     ├──────────────────────────────┤
│           │ [KPI 카드] 요약 3종          │
│           ├──────────────────────────────┤
│           │ [데이터 테이블] 결과 목록    │
└───────────┴──────────────────────────────┘
```

**1-4. 입출력 데이터 명세**
- 입력: 텍스트 폼 / CSV·Excel 업로드 / 이미지·문서 첨부 / 직접 입력 (다중 선택)
- 출력: 요약 카드 / 데이터 테이블 / 차트·그래프 / PDF 리포트 / 직접 입력 (다중 선택)

**1-5. 예외 상황 & 빈 화면**
- 데이터 없음: 안내 문구 / 등록 유도 버튼 / 직접 입력
- 에러·잘못된 입력: 토스트 알림 / 상단 에러 배너 / 직접 입력

### 6-2. Step 2 — 기술 및 저장 방식
1-1장 표의 추천값(Next.js + Tailwind, LocalStorage)에 기본 체크된 상태로 시작한다.
`[✨ 잘 모르겠어요 (기본값 적용)]` 버튼은 Step 2 전체를 추천값으로 되돌린다.
`[🤖 AI 추천 받기]`를 누르면 기술 선택 자체를 비우고, "요구사항을 보고 Agent가 스택을 제안하라"는 지시문으로 출력한다(2-2-4 참조).

### 6-3. Step 3 — AI 규칙 & 가드레일
1-2장의 G1~G12를 체크박스 목록으로 노출하고 **G1~G9는 기본 체크** 상태로 시작한다.
G10은 민감 데이터 선택 시 자동 체크·잠금.
G11(Agent 범용성)·G12(배포 & 소스 보호)는 **VC Planner 자체 개발 규칙**이므로, 사용자 프로젝트용 체크리스트에서는 기본 해제 상태의 선택 항목으로 노출한다(원하면 켤 수 있음).
하단에 `[➕ 가드레일 직접 추가]`를 배치한다.

### 6-4. Step 4 — 최종 생성 & 추출

| 버튼 | 동작 |
|---|---|
| `[📋 전체 프롬프트 복사]` | `Prompt.md` 전문을 클립보드로 (도구 비종속 표준 Markdown) |
| `[📄 Agent 규칙 파일 복사]` | Step 3 규칙만 추려서 클립보드로. 대상 도구를 고르면 `.cursorrules` / `AGENTS.md` / `.windsurfrules` 등 파일명만 바뀌고 **내용은 동일** |
| `[💾 txt/md 다운로드]` | UTF-8 Blob으로 파일 저장 |
| `[🖨️ PDF 리포트 다운로드]` | 명세 + 와이어프레임 + 플로우차트 포함 A4 PDF |

**위임 항목 요약 배너**: `delegated === true`인 항목이 있으면 화면 상단에 "N개 항목을 Agent 판단에 맡겼습니다"를 표시하고, 클릭 시 해당 항목 목록을 펼친다.

**PDF 주의사항**: `html2pdf.js`는 DOM을 이미지로 캡처하므로 한글 폰트 임베딩 문제가 없다. 다만 캔버스 기반 에디터는 화면 밖 영역이 잘릴 수 있으므로, **인쇄 전용 레이아웃 컴포넌트**를 별도로 만들어 캡처한다.

**다운로드 인코딩**: `Blob`에 `type: 'text/markdown;charset=utf-8'`을 지정하고 BOM은 붙이지 않는다. `.cursorrules`는 BOM이 파싱을 방해할 수 있어 반드시 BOM 없이 저장한다. 상세는 3-5장.

---

## 7. 단계별 구현 계획

각 Phase는 **완료 조건(DoD)을 충족하고 승인을 받은 뒤** 다음으로 넘어간다 [G5].

### Phase 0 — 프로젝트 골격
- Next.js(App Router) + TypeScript + Tailwind 초기화, 다크 모드 기본 적용
- 3장 버전 정책대로 의존성 설치 (설치 전 승인 [G8])
- **12-1장 `next.config.ts` Static Export 설정을 처음부터 적용** — 정적 배포와 충돌하는 코드가 나중에 쌓이지 않게 한다 [G12]
- `.editorconfig` / `.gitattributes` 추가 (3-5장 인코딩 규칙) [G1]
- 라이선스 푸터 컴포넌트 자리 확보 (문구는 Phase 8에서 확정)
- 레이아웃 셸: 헤더 + 진행바 + 좌우 2단 + 하단 네비게이션 + 푸터
- Step 1~4 빈 카드와 라우팅(또는 상태 기반 전환)
- **DoD**: ① `npm run dev`로 4개 Step 사이를 오갈 수 있다 ② `npm run build`가 `out/`을 생성한다

### Phase 1 — 공통 입력 컴포넌트
- `ChoiceGroup` 구현: 라디오/체크박스/드롭다운 모드 + `[✏️ 직접 입력]` 토글 + 툴팁 + `[✨ 추천]` 배지
- `[🤖 AI 추천 받기]` 위임 토글 + 선호 힌트 입력창 (5-1장 `delegated` / `delegateHint`)
- 전역 상태(Context + `useReducer`), LocalStorage 자동 저장 훅, 하이드레이션 가드
- **DoD**: 아무 Step에서나 `ChoiceGroup`을 얹으면 선택·직접입력·위임 토글·새로고침 복원이 모두 동작한다

### Phase 2 — Step 1 폼 + 미리보기 기반
- 1-1 기본 정보, 1-4 입출력, 1-5 예외 처리 구현
- 마크다운 생성기 1차 버전 + 우측 실시간 미리보기
- 민감 데이터 선택 → Step 3 G10 자동 잠금 연동
- **DoD**: 입력을 바꾸면 미리보기가 즉시 갱신되고, 직접 입력 내용이 누락 없이 반영된다

### Phase 3 — Step 2 & Step 3
- 기술/저장 선택, `[✨ 기본값 적용]` 버튼
- 가드레일 체크리스트, `[➕ 직접 추가]`
- 마크다운 생성기에 기술 스택·규칙 섹션 추가
- **DoD**: Step 2·3 선택이 미리보기의 해당 섹션에 반영된다

### Phase 4 — Flowchart 에디터
- React Flow 캔버스, 5종 커스텀 노드, 팔레트 드래그 추가, 연결·삭제·이름 편집
- `[✨ 기본 CRUD 흐름]` 프리셋
- 노드/엣지 → 텍스트 흐름 + Mermaid 직렬화
- **DoD**: 그린 흐름이 새로고침 후에도 복원되고 미리보기에 Mermaid로 나타난다

### Phase 5 — Wireframe 에디터
- `react-grid-layout` 캔버스, 7종 컴포넌트 팔레트, 배치·리사이즈·라벨 편집·삭제
- 레이아웃 → ASCII 도식 + 컴포넌트 목록표 변환기
- **DoD**: 배치를 바꾸면 ASCII 도식이 형태를 따라 바뀐다

### Phase 6 — Step 4 출력
- 11장 표준 구조에 맞춘 `Prompt.md` 최종 조립, Agent 규칙 파일 추출 로직
- 위임(`delegated`) 항목 → 위임 지시문 렌더링, 상단 요약 배너
- 도구별 부록(`.cursorrules` / `AGENTS.md` / `.windsurfrules`) 파일명 매핑
- 복사 / md·txt 다운로드 (UTF-8, BOM 없음)
- **DoD**: ① 복사·다운로드한 파일을 Windows 메모장에서 열어도 한글이 깨지지 않는다 [G1] ② 생성물에 도구 전용 문법이 본문에 섞이지 않는다 [G11]

### Phase 7 — PDF 리포트 & 마감
- 인쇄 전용 레이아웃, `html2pdf.js` A4 출력 (플로우차트·와이어프레임 포함)
- 빈 상태·에러 처리, 반응형 점검, 접근성(키보드 포커스·대비) 점검
- README 작성 (라이선스 성격을 "source-available"로 정확히 표기, 12-3장)
- **DoD**: PDF에 명세 전문과 두 다이어그램이 모두 잘리지 않고 담긴다

### Phase 8 — 배포 · 라이선스 · 소스 보호 [G12]
- `next.config.ts`에 Static Export 구성 적용 (`output: 'export'`, `images.unoptimized`, `productionBrowserSourceMaps: false`) — 12-1장
- `npm run build` 결과 `out/` 정적 산출물 생성 확인, 로컬에서 정적 서버로 구동 검증
- Cloudflare Pages 배포 설정 문서화 (빌드 명령 / 출력 디렉터리 / `_headers`) — 12-2장
- `LICENSE`(BUSL-1.1) + `NOTICE` 추가, 푸터 라이선스 고지 컴포넌트, 생성물 하단 출처 문구 — 12-3장
- 프로덕션 번들에서 소스맵·주석 제거 확인 — 12-4장
- **DoD**: ① `out/`을 그대로 올려 동작한다 ② 배포본 DevTools에 원본 소스맵이 노출되지 않는다 ③ 푸터에 라이선스 고지가 보인다

---

## 8. 완료 기준 (전체 DoD)

1. 마우스 클릭만으로 Step 1~4를 끝까지 완주할 수 있다.
2. 모든 주요 선택 항목에 `[✏️ 직접 입력 / 기타]`가 있고, 그 내용이 출력물에 반영된다.
3. 플로우차트와 와이어프레임이 각각 Mermaid / ASCII로 자동 변환된다.
4. 4종 출력(복사·rules 복사·파일 다운로드·PDF)이 모두 동작한다.
5. 새로고침 후에도 작업 내용이 유지된다.
6. 한글이 어느 출력물에서도 깨지지 않는다.
7. 외부 네트워크 요청이 0건이다(전 기능 브라우저 내 완결).
8. `[🤖 AI 추천 받기]`로 위임한 항목이 출력물에 **Agent 판단 요청 지시문**으로 나타난다.
9. 생성된 `Prompt.md`가 특정 도구 전용 문법 없이 표준 Markdown만으로 구성된다 [G11].
10. `npm run build`가 `out/` 정적 산출물을 만들고, Cloudflare Pages에 그대로 배포 가능하다 [G12].
11. 프로덕션 배포본에 `.map` 소스맵이 없고, 푸터에 라이선스 고지가 표시된다 [G12].

---

## 9. 리스크 및 대응

| 리스크 | 영향 | 대응 |
|---|---|---|
| 웹의 `react-grid-layout` 예제 대부분이 v1 기준 | 동작하지 않는 코드 작성 | v2 공식 문서 기준으로 작성. `verticalCompact` 등 제거된 API 사용 금지 |
| 라이브러리 peer dependency 충돌 | 설치 실패 | `--legacy-peer-deps`로 덮지 않고 원인 보고 후 승인 [G2][G8] |
| 캔버스 라이브러리 SSR 오류 | 빌드·실행 실패 | `next/dynamic({ ssr: false })` + `'use client'` |
| LocalStorage 하이드레이션 불일치 | 콘솔 경고·화면 깜빡임 | `mounted` 가드 공통 훅 |
| PDF에서 캔버스 영역 잘림 | 리포트 품질 저하 | 인쇄 전용 레이아웃 별도 구성 후 캡처 |
| ASCII 변환 품질 | 레이아웃이 잘 안 읽힘 | 그리드 12열 기준 단순화. 복잡하면 컴포넌트 목록표를 주 수단으로 |
| LocalStorage 용량(약 5MB) 초과 | 저장 실패 | 저장 실패를 감지해 안내하고 JSON 내보내기를 유도 |
| 기능 욕심으로 인한 범위 확대 | 미완성 | Phase 경계 엄수 [G5], 10장 비범위 준수 |
| Static Export와 호환되지 않는 기능 도입(API Routes·서버 액션·`next/image` 최적화) | 배포 단계에서 빌드 실패 | Phase 0부터 `output: 'export'`를 켜 두어 위반을 즉시 감지 [G12] |
| 추가 난독화 도구가 Turbopack/Next 빌드와 충돌 | 빌드 파손, 디버깅 난이도 급증 | 기본은 SWC minify + 소스맵 비활성화까지만. 추가 난독화는 선택 과제이며 사전 승인 필요 [G8] |
| 난독화로 코드 보호가 가능하다는 오해 | 잘못된 보안 기대 | 12-4장에 한계를 명시. 클라이언트 코드는 완전 보호 불가이며 라이선스가 주된 방어 수단 |
| 특정 도구 전용 문법이 본문에 섞임 | 다른 Agent에서 오해석 | 본문은 표준 Markdown만, 도구 의존 내용은 부록으로 분리 [G11] |

---

## 10. 비범위 (Out of Scope, v1)

- 회원가입·로그인·서버 저장·팀 공유
- 외부 AI API 연동 (`[🤖 AI 추천 받기]`는 호출이 아니라 **Agent 위임 마커**, `[✨ 기본값 적용]`은 로컬 프리셋)
- 생성한 명세로 실제 코드를 만들어주는 기능
- 다국어 지원 (v1은 한국어 전용)
- 실시간 협업 편집
- 서버 사이드 라이선스 검증·인증 (Static Export이므로 불가. 라이선스는 문서·고지로만 방어)

---

## 11. Agent 범용성 — 표준 프롬프트 포맷 [G11]

### 11-1. 원칙

산출물은 **어느 AI Coding Agent에 붙여넣어도 같은 뜻으로 읽히는 문서**여야 한다.

| 지켜야 할 것 | 피해야 할 것 |
|---|---|
| CommonMark 표준 문법 (제목, 목록, 표, 코드 펜스) | 도구 전용 지시자 (`@file`, `@Codebase`, `<cursor:...>` 등) |
| 평문 파일 경로 표기 (`src/app/page.tsx`) | IDE 전용 링크·자동 참조 문법 |
| 명시적 문장으로 쓴 역할·제약 | 특정 모델만 아는 은어·약어 |
| Mermaid 코드 펜스 (```` ```mermaid ````) | 이미지·바이너리 첨부 의존 |
| ASCII 박스 드로잉 | 렌더러가 필요한 커스텀 위젯 |

- **YAML Front Matter는 본문에 넣지 않는다.** 일부 도구는 무시하고 일부는 메타로 파싱해 결과가 갈린다. 필요한 메타 정보는 본문 표로 적는다.
- 이모지는 가독성 보조로만 쓰고, 이모지 없이도 의미가 통하게 문장을 쓴다.
- 문서는 **자기완결적(self-contained)** 이어야 한다. 외부 링크를 열어야만 이해되는 문장을 쓰지 않는다.

### 11-2. `Prompt.md` 표준 섹션 구조

아래 8개 섹션 순서를 고정한다. 최신 Agent들이 공통적으로 잘 따르는 "역할 → 맥락 → 요구사항 → 제약 → 작업 순서 → 완료 기준" 흐름이다.

```markdown
# {서비스명} — 개발 명세서

## 0. 이 문서 사용법
이 문서는 AI 코딩 Agent에게 전달하는 개발 지시서입니다.
특정 도구에 종속되지 않는 표준 Markdown으로 작성되었으며,
Cursor / Claude Code / Windsurf / Lovable / Bolt.new 등에서 그대로 사용할 수 있습니다.

## 1. 역할 (Role)
당신은 {역할}입니다. 아래 명세를 기준으로 작업하세요.

## 2. 프로젝트 개요 (Context)
- 서비스명 / 한 줄 설명 / 서비스 분야 / 대상 사용자 / 민감 데이터 여부

## 3. 요구사항 (Requirements)
### 3-1. 업무 흐름   ← 텍스트 흐름 + Mermaid
### 3-2. 화면 구성   ← ASCII 도식 + 컴포넌트 표
### 3-3. 입출력 데이터
### 3-4. 예외 상황 처리

## 4. 기술 스택 및 저장 방식 (Tech Constraints)

## 5. 작업 규칙 (Guardrails)
반드시 지켜야 할 규칙 목록. 위반이 필요하면 먼저 물어보세요.

## 6. 판단 위임 항목 (Decisions Delegated to You)
사용자가 결정을 보류한 항목입니다. 제안 후 확인을 받고 진행하세요.

## 7. 작업 순서 (Suggested Plan)
한 번에 전부 만들지 말고 단계별로 진행하세요.

## 8. 완료 기준 (Definition of Done)
```

- 섹션 6은 위임 항목이 **하나도 없으면 통째로 생략**한다. 빈 섹션은 Agent에게 노이즈다.
- 섹션 7은 Step 1 플로우차트 순서를 기반으로 자동 생성하되, 사용자가 수정할 수 있다.

### 11-3. 위임 항목 출력 템플릿

`delegated === true`인 항목은 섹션 6과 해당 본문 위치 양쪽에 나타난다. 본문에서는 한 줄 마커로, 섹션 6에서는 상세 지시로 쓴다.

**본문 인라인**

```markdown
- **데이터 저장 방식**: 🤖 판단 위임 (6장 참조)
```

**섹션 6 상세**

```markdown
## 6. 판단 위임 항목 (Decisions Delegated to You)

아래 항목은 사용자가 결정을 보류했습니다. 임의로 확정하지 말고,
**선택지 2~3개와 각각의 장단점을 제시한 뒤 추천안을 하나 골라** 확인을 받고 진행하세요.

### 6-1. 데이터 저장 방식
- 현재 상태: 미정
- 사용자 선호: 비용은 최소로, 회원가입은 없었으면 함
- 판단에 참고할 맥락: 서비스 분야=일반 IT, 민감 데이터=포함하지 않음
```

- "사용자 선호"는 `delegateHint`가 비어 있으면 줄 자체를 출력하지 않는다.
- "판단에 참고할 맥락"에는 Step 1의 분야·민감 데이터 여부를 자동으로 채운다.

### 11-4. 도구별 부록 (선택)

본문은 공통, 도구 차이는 **부록과 파일명 매핑**으로만 흡수한다. **내용은 전부 동일하고 파일 이름만 다르다.**

| 도구 | 규칙 파일명 | 배치 위치 |
|---|---|---|
| Cursor | `.cursorrules` (또는 `.cursor/rules/*.mdc`) | 프로젝트 루트 |
| Claude Code | `CLAUDE.md` 또는 `AGENTS.md` | 프로젝트 루트 |
| Windsurf | `.windsurfrules` | 프로젝트 루트 |
| Lovable / Bolt.new | 별도 파일 없음 — `Prompt.md` 전문을 채팅에 붙여넣기 | — |
| 그 외 | `AGENTS.md` | 프로젝트 루트 |

`output.includeToolAppendix`가 켜져 있을 때만 `Prompt.md` 맨 끝에 아래 부록을 덧붙인다.

```markdown
---
## 부록 A. 도구별 적용 방법
- Cursor: 5장 내용을 프로젝트 루트 `.cursorrules`로 저장하세요.
- Claude Code: 5장 내용을 `CLAUDE.md`로 저장하세요.
- Windsurf: 5장 내용을 `.windsurfrules`로 저장하세요.
- Lovable / Bolt.new: 이 문서 전문을 첫 메시지로 붙여넣으세요.
```

---

## 12. 배포 · 라이선스 · 소스 보호 [G12]

### 12-1. Next.js Static Export 설정

`next.config.ts`를 아래 형태로 고정한다. **Phase 0부터 적용**해서, 정적 배포와 호환되지 않는 코드가 들어오는 즉시 빌드가 깨지도록 만든다.

```ts
import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // Cloudflare Pages 등 정적 호스팅용 산출물(out/)을 생성한다
  output: 'export',

  // 정적 내보내기에서는 Next.js 이미지 최적화 서버를 쓸 수 없다
  images: { unoptimized: true },

  // 프로덕션 소스맵 비활성화 — 원본 코드 노출 방지
  productionBrowserSourceMaps: false,

  // 정적 호스팅에서 새로고침 404를 피하기 위해 디렉터리형 URL 사용
  trailingSlash: true,

  // 프로덕션 번들에서 console.* 호출 제거 — 내부 상태 노출 방지
  // console.error는 남겨 두어야 실사용 중 오류 신고를 받을 수 있다
  compiler: {
    removeConsole:
      process.env.NODE_ENV === 'production' ? { exclude: ['error'] } : false,
  },
};

export default nextConfig;
```

**Static Export에서 금지되는 것** (도입 시 [G12] 위반)

- API Routes (`app/api/**/route.ts`), 서버 액션(Server Actions)
- `next/image`의 기본 최적화 로더, `next/font` 원격 폰트 런타임 최적화
- 미들웨어(`middleware.ts`), ISR / `revalidate`, 동적 라우트의 런타임 생성
- 쿠키·헤더 기반 서버 분기 (`cookies()`, `headers()`)

이 프로젝트는 백엔드가 없는 완결형 클라이언트 앱이므로 위 제약이 실질적인 손해가 되지 않는다.

### 12-2. Cloudflare Pages 배포 설정

| 항목 | 값 |
|---|---|
| 프레임워크 프리셋 | Next.js (Static HTML Export) |
| 빌드 명령 | `npm run build` |
| 빌드 출력 디렉터리 | `out` |
| Node 버전 | 환경 변수 `NODE_VERSION = 20` 이상 (로컬은 24.18.0) |
| 환경 변수 | 없음 (비밀값 없음 [G10]) |

`public/_headers`로 기본 보안 헤더를 준다. 정적 배포이므로 서버 코드 없이 헤더만으로 처리한다.

```
/*
  X-Content-Type-Options: nosniff
  X-Frame-Options: DENY
  Referrer-Policy: strict-origin-when-cross-origin
  Permissions-Policy: geolocation=(), microphone=(), camera=()
```

`/*.map`을 차단하는 규칙은 넣지 않는다. 애초에 소스맵을 만들지 않는 것이 정답이고, 없는 파일을 막는 규칙은 오해를 부른다.

### 12-3. 라이선스 정책

**목표**: 웹 서비스는 누구나 자유롭게 **사용**하되, 소스코드의 **무단 재배포·재가공·상업적 재호스팅**은 막는다.

**채택 확정: BUSL-1.1 (Business Source License 1.1)** — 사용자 승인 완료

- 소스는 공개하되 상업적/프로덕션 사용을 제한할 수 있다.
- Additional Use Grant로 "개인·비상업적 사용 및 내부 평가"는 허용한다.
- Change Date(예: 배포 후 4년) 이후 Apache-2.0으로 자동 전환된다.
- 주의: BUSL-1.1은 **OSI 승인 오픈소스가 아니다.** README에 "source-available"이라고 정확히 표기하고 오픈소스라 부르지 않는다.

대안(사용자 선택 시 교체 가능)

| 라이선스 | 성격 | 적합한 경우 |
|---|---|---|
| **BUSL-1.1** (권장) | source-available, 기간 후 오픈소스 전환 | 코드는 보여주되 재호스팅은 막고 싶을 때 |
| Elastic License 2.0 | 관리형 서비스 재판매 금지 | 조항이 짧고 읽기 쉬운 걸 원할 때 |
| CC BY-NC-ND 4.0 | 비상업·변경금지 | 코드가 아닌 문서·콘텐츠 보호가 주 목적일 때 (소프트웨어에는 비권장) |
| All Rights Reserved | 저장소 공개 + 라이선스 미부여 | 가장 강한 제한. 다만 기여·신뢰 확보는 어려움 |

**배치 위치 (전부 필수)**

1. 루트 `LICENSE` — BUSL-1.1 전문 + Licensor / Change Date / Change License / Additional Use Grant 채움
2. 루트 `NOTICE` — 서드파티 오픈소스 라이선스 목록 (React, Next.js 등 MIT 고지 의무 이행)
3. `README.md` — "source-available, 무단 재배포·재가공 금지" 한 문단
4. **웹 푸터** — 전 화면 공통 컴포넌트

```
© 2026 VC Planner · Licensed under BUSL-1.1 (source-available)
무단 재배포 및 재가공을 금지합니다 · 입력하신 내용은 브라우저에만 저장되며 서버로 전송되지 않습니다
```

5. **생성 산출물 하단 (`Prompt.md` 마지막 줄)** — 여기서 경계를 분명히 한다.

```markdown
---
_이 문서는 VC Planner로 생성되었습니다. **문서의 내용과 저작권은 작성자 본인에게 있으며 자유롭게 사용·수정·배포할 수 있습니다.**
VC Planner 자체의 소스코드는 BUSL-1.1로 보호되며 무단 재배포·재가공을 금합니다._
```

> **중요**: 사용자가 만든 명세서에는 VC Planner의 라이선스가 전파되지 않는다. 산출물은 사용자 소유다. 이 점을 문구에 명시해 오해와 사용 기피를 막는다.

### 12-4. 소스코드 보호 — 범위와 한계

**적용할 것**

| 조치 | 방법 | 효과 |
|---|---|---|
| 소스맵 비활성화 | `productionBrowserSourceMaps: false` (Next.js 기본값이지만 명시) | 원본 파일·주석·변수명 복원 차단 |
| 코드 최소화 | Next.js 프로덕션 빌드의 SWC minifier (기본 활성) | 변수명 축약, 공백·주석 제거 |
| 콘솔 로그 제거 | `compiler.removeConsole` | 내부 상태 로그 노출 방지 |
| 개발 산출물 제외 | `.gitignore`에 `out/`, `.next/` | 빌드 산출물 커밋 방지 |
| 라이선스 고지 | 12-3장 | **법적 방어 — 실질적으로 가장 강력한 수단** |

**적용하지 않을 것 (기본값)**

`javascript-obfuscator` / `webpack-obfuscator` 같은 강한 난독화는 **기본 적용하지 않는다.** 이유는 다음과 같다.

- Next.js 15의 SWC 빌드 파이프라인·Turbopack과 충돌 사례가 있고, App Router 청크 분할과 상호작용이 검증되지 않았다.
- 번들 크기가 크게 늘고 런타임이 느려진다(문자열 배열 인코딩·제어 흐름 평탄화 비용).
- 프로덕션 버그 재현이 사실상 불가능해진다.
- **보호 효과는 제한적이다.** 브라우저에 내려간 코드는 원리상 완전히 숨길 수 없다.

필요하다고 판단되면 Phase 8 이후 별도 과제로 다루고, 착수 전 승인을 받는다 [G8].

**정직한 결론**: 이 앱은 100% 클라이언트에서 동작하므로 로직을 기술적으로 완전히 숨길 수 없다. 실질적 방어선은 ① 라이선스 고지 ② 커밋 이력에 의한 저작 증명 ③ 브랜드·서비스 완성도이며, 난독화는 보조 수단이다.

---

## 13. 다음 액션

1. ~~3-2 버전 정책(**Next.js 15 + React 19**)과 3-0 환경 정보 확정~~ ✅ 완료
2. ~~12-3장 라이선스를 **BUSL-1.1**로 확정~~ ✅ 완료 — 단, `LICENSE` 작성에 필요한 **Licensor 표기명**과 **Change Date**는 미정
3. ~~Phase 0 착수. 12-1장 `next.config.ts` 설정을 함께 적용한다.~~ ✅ 완료
4. ~~Phase 0 완료 시점에 1-2장 가드레일(G1~G12)을 `.cursorrules` + `AGENTS.md`로 생성한다.~~ ✅ 완료
5. Phase 0 승인 후 Phase 1(공통 입력 컴포넌트)로 넘어간다.

### Phase 0 결과 메모

- 폴더명 `VC_Planner`에 대문자가 있어 `create-next-app`이 거부한다. 소문자 임시 폴더에 생성 후 이동하는 방식으로 처리했고, `package.json`의 `name`은 `vc-planner`다.
- git 저장소를 프로젝트 폴더에 새로 만들었다(이전에는 홈 디렉터리 저장소가 잡혀 있었다).
- 스캐폴딩 결과: Next.js 15.5.22 / React 19.1.0 / Tailwind v4 / 개발 서버는 webpack.
- `npm audit` 고위험 12건은 전부 **빌드·개발 전용 의존성**(eslint 계열의 `minimatch`, Next 내부 `postcss`, `sharp`)이다. `npm audit fix --force`는 `next@9.3.3` 다운그레이드와 `eslint@10` 업그레이드를 시도하므로 실행하지 않는다 [G2]. 정적 배포 산출물에는 영향이 없다.
- 한글 폰트: Geist에는 한글 글리프가 없어 `--font-sans` 스택 뒤에 `Pretendard` → `Malgun Gothic` → `Apple SD Gothic Neo`를 이어 붙였다.
